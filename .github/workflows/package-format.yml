name: 🔍 Format Package

on:
    workflow_call:
        inputs:
            files:
                description: File patterns to format using Prettier (e.g., "**/*.{yml,yaml,json,md,js,ts,css,html,sh}")
                required: false
                type: string
                default: ""
            dotnet-projects:
                description: Array of .NET project paths to format (e.g., ["src/Core", "src/Godot"])
                required: false
                type: string
                default: "[]"
            global-json-file:
                description: The relative path to the `global.json` file to extract .NET version.
                required: false
                type: string
                default: ""

jobs:
    format_files:
        if: inputs.files != ''
        runs-on: ubuntu-latest

        steps:
            - name: 📂 Checkout Code
              uses: actions/checkout@v5

            - name: 🔍 Format Files
              uses: grovegs/actions/.github/actions/format-files@v0.1.3
              with:
                  files: ${{ inputs.files }}

    format_dotnet_projects:
        if: fromJSON(inputs.dotnet-projects)[0] != null
        runs-on: ubuntu-latest
        strategy:
            matrix:
                project: ${{ fromJSON(inputs.dotnet-projects) }}

        steps:
            - name: 📂 Checkout Code
              uses: actions/checkout@v5

            - name: ⚙️ Setup Dotnet
              uses: grovegs/actions/.github/actions/setup-dotnet@v0.1.3
              with:
                  global-json-file: ${{ inputs.global-json-file }}
                  cache: ${{ github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' }}

            - name: 🔍 Format .NET Project
              uses: grovegs/actions/.github/actions/format-dotnet@v0.1.3
              with:
                  project: ${{ matrix.project }}
